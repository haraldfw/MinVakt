roles:
  admin: Admin

spring:
  thymeleaf.cache: false

  datasource:
    url: jdbc:mysql://mysql.stud.iie.ntnu.no:3306/g_scrum05
    username: g_scrum05
    password: ENC(q41c+P9rkQ1S8nxLO3mM5HzaA1LJ0bdu)
    driver-class-name: com.mysql.jdbc.Driver
    tomcat:
      max-active: 2
      connection-properties: useUnicode=true;characterEncoding=utf-8;max_allowed_packet=16000000;

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    properties.hibernate:
      current_session_context_class: org.springframework.orm.hibernate4.SpringSessionContext
      show_sql: false
      format_sql: true
      use_sql_comments: true

  mail:
    host: smtp.gmail.com
    username: scrum5.noreply@gmail.com
    password: ENC(vKmz72/PkSJ+RWAv7dQSo0pMf/MfYKX7)
    port: 587
    properties.mail.smtp:
      socketFactory.port: 25
      starttls:
        enable: true
        required: true

jasypt:
    encryptor:
        bean: hibernateEncryptor
        password: ${SCRUM5_ENCPW:}